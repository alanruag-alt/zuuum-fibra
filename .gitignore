# dependencias
/node_modules
/.pnp
.pnp.js

# compilación
/.next/
/out/
/build
next-env.d.ts

# pruebas
/coverage

# variables de entorno — NUNCA se suben
.env
.env.local
.env*.local
.env.production
# cualquier respaldo/copia de un .env tampoco (p. ej. .env.local.respaldo)
.env*.respaldo
.env.*.bak
.env.local.*

# sistema
.DS_Store
*.pem
npm-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo

# respaldos y datos reales de clientes
/datos-reales/
*.xlsx
*.csv

# Carpeta de descarte: el puente no puede borrar, solo mover aqui.
_to_delete/

# La ruta de descarga se llama «fusiones.xlsx» y es una carpeta, no un Excel:
# el patrón *.xlsx de arriba se la comía y el archivo nunca llegaba al repo.
!**/fusiones.xlsx/
!**/fusiones.xlsx/**
